# SchiferQRX

SchiferQRX — это веб-приложение на Django для создания и извлечения двухслойных QR-кодов с использованием LSB-стеганографии. Проект включает систему аутентификации пользователей, личные кабинеты, статистику активности, систему достижений и поддержку ограничения времени жизни скрытого сообщения.

> 🏆 Проект занял **1 место на Сократовских чтениях** (Кафедра информационных технологий, Московский международный университет, апрель 2026).

## Возможности

- регистрация и авторизация пользователей;
- генерация QR-кодов с открытым и скрытым текстом (двухслойное кодирование);
- извлечение скрытого текста из специально созданных QR-кодов;
- поддержка метаданных в формате JSON (версия, срок действия);
- ограничение времени жизни скрытого сообщения (от 1 минуты до 30 дней);
- личный кабинет с отображением статистики (количество генераций, декодирований, скачиваний);
- график активности пользователя (кольцевая диаграмма);
- система достижений (ачивки) и история действий;
- администрирование через встроенную панель Django;
- готовая контейнеризация (Docker, docker-compose) для развёртывания на сервере.

## Принцип работы

В основе метода лежит стеганография LSB (Least Significant Bit). При генерации сначала создаётся обычный QR-код из открытого текста. Затем в младшие биты цветовых каналов каждого пикселя встраивается JSON пакет, содержащий секретное сообщение и, при необходимости, абсолютную метку времени истечения срока. Визуально модифицированный QR-код неотличим от обычного, но позволяет извлекать скрытый слой с помощью специального декодера и вотличии от специалезированных типов читаем для обычных устройств. Уровень коррекции ошибок H (30%) обеспечивает устойчивость к повреждениям до 20% площади кода.

## Стек технологий

- Python 3.9
- Django 3.2.16
- PostgreSQL 13 (в продакшене), SQLite3 (для разработки)
- Gunicorn
- Nginx
- Docker, docker-compose
- HTML, CSS, JavaScript, Chart.js
- Библиотеки: qrcode, Pillow, whitenoise, dj-database-url, psycopg2-binary

## Структура проекта

```bash
SchiferQRX/
├── qr_secret/                     # Корень Django-проекта
│   ├── qr_secret/                 # Настройки проекта (settings.py, urls.py)
│   ├── qrapp/                     # Приложение с QR-логикой
│   │   ├── services/
│   │   │   └── qr_generator.py    # LSB-алгоритмы (встраивание, извлечение, TTL)
│   │   ├── views.py               # API для генерации и декодирования
│   │   └── urls.py
│   ├── users/                     # Приложение пользователей
│   │   ├── models.py              # UserProfile, Achievement, UserActivity
│   │   ├── views.py               # Регистрация, профиль, трекинг скачиваний
│   │   └── urls.py
│   ├── static/                    # CSS, JS (auth.css, style.css, app.js)
│   ├── templates/                 # HTML-шаблоны (index.html, profile.html, login.html)
│   └── manage.py
├── Dockerfile                     # Сборка образа Django + Gunicorn
├── docker-compose.yml             # Описание сервисов (web, db)
├── requirements.txt               # Зависимости Python
├── .env.example                   # Шаблон переменных окружения
└── README.md
